services:

  app:
    build: .
    ports:
      - "7860:7860"
    env_file:
      - .env
    environment:
      - REDIS_HOST=redis
    depends_on:
      - redis
  
  redis:
    image: redis/redis-stack:latest
    ports:
      - "6379:6379"  # The database port
      - "8001:8001"  # RedisInsight (The UI)